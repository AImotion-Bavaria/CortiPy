# TODO Make the scalp rtopology also a live with a heatmap with the data from the electdrodes and them from the respective location

# Todo make Cortipy read the file from matlab ( send via Mail)

# TODO Also we need to remove the drag and drop scal topology again as we have the 10/20 notation so we can just make that in the electrodes table a dropdown for that i guess thats easier and then we just update the position laben

# TODO for Actichamp, pleas update it, so that for the impedances we fire it all up after selecting the device, then shutdown, then for start the measurement, we start it again so we dont keep it running all the time.


# TODO Implement Triggers and autostart on experiment if selected and configured
# Idea add stimulus and save the triggers along the data recording, make sure that they overlap perfectly so take account for any jittering or for the delay of data recieving
# D2 – Software-curated signals (Exp. 1)

Minimal two-channel synthetic recording stored as BIN:
- `ContinuousSine/ContinuousSine_continuous.bin` (sine + trigger)
- `ContinuousSine/params.json`

This dataset is used for EEGLAB parity checks and sine/PSD inspection utilities in `experiments/experiment1/`.

